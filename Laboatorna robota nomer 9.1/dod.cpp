// dod.cpp
// файл реалізації функції
#include "dod.h"
#include "var.h" // підключили зовнішні оголошення змінних
using namespace nsVar;
void nsDod::dod() 
{
	a *= ((-1) * (n + 1)) / ((2 * n) + 1) * (x * ((2 * n) - 1)); // доданок домножується на коефіцієнт рекурентності
}  

